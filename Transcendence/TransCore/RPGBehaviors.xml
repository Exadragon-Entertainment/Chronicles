<?xml version="1.0" encoding="utf-8"?>

<TranscendenceModule>
	
	<!-- Convoy Behavior
	
		USAGE NOTES
		
		This simulates the behavior of a freighter convoy escorted by
		defenders.
		
		EXTRA DATA
		
		behavior:			Ship's current behavior
								'avoidingEnemy			= We're trying to run away
								'onCourse				= Ship on straight course
	-->

	<ShipClass UNID="&evStdConvoyEncounterBehavior;"
			class=				"(convoy encounter behavior)"
			virtual=			"true"
			
			attributes=			"behaviorClass"
			>
			
		<Events>
			<GetCreatePos>
				(if aTargetObj
					(intComputeRandomEncounterPos aTargetObj)
					)
			</GetCreatePos>

			<OnBehavior>
				(block (behavior)
					(setq behavior (objGetData gSource "00003015_behavior"))
					(switch
						; If we see any enemy ships then run away
						(eq behavior 'onCourse)
							(block (theEnemy)
								(if (setq theEnemy (sysFindObject gSource "sEPAN"))
									(block (shelterObj gateObj)
										(shpCancelOrders gSource)
										
										(setq gateObj (sysFindObject gSource "GN -uncharted;"))
										(setq shelterObj (sysFindObject gSource "TJAN +populated; "))
										
										; If there's shelter nearby, go to it
										(if (and shelterObj
												(ls (objGetDistance gSource shelterObj) (objGetDistance gSource gateObj))
												)
											(block Nil
												(shpOrder gSource 'dock shelterObj)
												(shpOrder gSource 'waitForThreat (random 10 40))
												)
											)
											
										; Gate out
										(shpOrder gSource 'gate gateObj)
										
										(objSetData gSource "00003015_behavior" 'avoidingEnemy)
										)
									)
								)
						)
					)
			</OnBehavior>
			
			<OnCreateOrders>
				(block (theCourse)
					(if (not aTargetObj)
						(setq aTargeObj gPlayerShip)
						)
						
					; Move towards the player's path
					(setq theCourse (intComputeRandomEncounterCourse gSource aTargetObj))
					(shpOrder gSource 'holdCourse theCourse 300)
					
					; Register a timer so that we can look for enemies
					(sysAddObjRecurringTimerEvent 30 gSource "OnBehavior")
					(objSetData gSource "00003015_behavior" 'onCourse)
					)
			</OnCreateOrders>
			
			<OnOrdersCompleted>
				(block (behavior)
					(setq behavior (objGetData gSource "00003015_behavior"))
					(switch
						; If we're done with this order, then we never encountered
						; the player, so we're done, head for a stargate
						; Note: we might encounter the player en route
						(eq behavior 'onCourse)
							(shpOrder gSource 'gate)
						)
					)
			</OnOrdersCompleted>
		</Events>
	</ShipClass>
	
	<!-- Mining Behavior
	
		USAGE NOTES
		
		This simulates the behavior of a ship with a mining weapon mining an
		ore deposit.
		
		EXTRA DATA
		
		depositID: If non-Nil, this is the deposit we should mine.
								
	-->

	<ShipClass UNID="&evStdMiningBehavior;"
			class=				"(mining behavior)"
			virtual=			"true"
			
			attributes=			"behaviorClass"
			>
			
		<Events>
			<OnOrdersCompleted>
				(block (
					depositObj
					oreObj
					asteroidObj
					)
					
					(switch
						;	Is our cargo hold full? Then we're done.
						
						(ls (objGetCargoSpaceLeft gSource) 2000)
							(shpOrder gSource 'gate)
							
						;	Are we far from our target area? If so, we need to get
						;	closer.
						
						(and (setq depositObj (objGetObjByID (objGetData gSource 'depositID)))
								(gr (objGetDistance gSource depositObj) 50)
								)
							(shpOrder gSource 'approach depositObj 25)
						
						;	If there is any ore in the area, then loot it.
						
						(and (setq oreObj (sysFindObject gSource "TN +minedOre;"))
								(leq (objGetDistance gSource oreObj) 100)
								(objGetItems oreObj "* +ore; | * +minable;")
								)
							(shpOrder gSource 'loot oreObj)
							
						;	Are there any asteroids nearby with discovered ore? If so,
						;	then we mine them.

						(setq asteroidObj (random
								(filter (sysFindObject gSource "tN:50 +asteroid") theObj
									;   LATER: API 30 allows passing a criteria to objGetOverlays
									(filter (objGetOverlays theObj) theOverlayID
										(typHasAttribute (objGetOverlayType theObj theOverlayID) 'oreCount)
										)
									)
								))
							(shpOrder gSource 'attack asteroidObj (random 5 10))
							
						;	Otherwise, look for any asteroid in range that we haven't
						;	already checked.
						
						(setq asteroidObj (random 
								(filter (sysFindObject gSource "tN:50 +asteroid") theObj
									(not (objGetData gSource (cat "00010050_Checked_" (objGetID theObj))))
									)
								))
							(block Nil
								(shpOrder gSource 'attack asteroidObj (random 5 10))
								(objSetData gSource (cat "00010050_Checked_" (objGetID asteroidObj)) True)
								)
							
						;	Otherwise, there's nothing to do
						
						(shpOrder gSource 'gate)
						)
					)
			</OnOrdersCompleted>
			
			<OnAttacked>
				(block (depositObj)
					(switch
						;	If no one is responsible, then nothing to do
					
						(not aOrderGiver)
							Nil
							
						;	If this is a friend, then ignore
						
						(= (objGetDisposition gSource aOrderGiver) 'friend)
							Nil
						
						;	If we're already attacking this target, then nothing to do
					
						(= (shpGetOrderTarget gSource) aOrderGiver)
							Nil
						
						;	If we're not near our target area, then nothing to do
					
						(and (setq depositObj (objGetObjByID (objGetData gSource 'depositID)))
								(gr (objGetDistance gSource depositObj) 50)
								)
							Nil
						
						;	Retaliate
					
						(block Nil
							(shpCancelOrders gSource)
							(shpOrder gSource 'attack aOrderGiver (random 10 20))
							)
						)
					)
			</OnAttacked>
		</Events>
	</ShipClass>
	
	<!-- Patrol Encounter Behavior
	
		USAGE NOTES
		
		This simulates the behavior of a ship on patrol that detects the
		player in a random encounter.

		This is not a real cyclic patrol - it attacks hostiles nearby to its path
		on the way to attempt intercepting the player. If it fails to intercept
		the player in time it leaves via a gate.
		
		EXTRA DATA
		
		behavior:			Ship's current behavior
								'attackingEnemy			= We're engaging the enemy
								'onCourse				= Ship on straight course
	-->

	<ShipClass UNID="&evStdPatrolEncounterBehavior;"
			class=				"(patrol encounter behavior)"
			virtual=			"true"
			
			attributes=			"behaviorClass"
			>
			
		<Events>
			<GetCreatePos>
				(if aTargetObj
					(intComputeRandomEncounterPos aTargetObj)
					)
			</GetCreatePos>

			<OnBehavior>
				(block (behavior)
					(setq behavior (objGetData gSource "00003014_behavior"))
					(switch
						; If we see any enemy ships then attack
						(eq behavior 'onCourse)
							(block (theEnemy)
								(if (setq theEnemy (sysFindObject gSource "sEPAN"))
									(block Nil
										(shpCancelOrders gSource)
										(shpOrder gSource 'attack theEnemy)
										(shpOrder gSource 'attackNearestEnemy)
										(objSetData gSource "00003014_encounterPos" (objGetPos gSource))
										(objSetData gSource "00003014_behavior" 'attackingEnemy)
										)
									)
								)

						; If the target has run away, then don't chase
						(eq behavior 'attackingEnemy)
							(if (and (geq (sysVectorDistance (objGetData gSource "00003014_encounterPos") (objGetPos gSource)) 300)
									(geq (objGetDistance gSource (objGetTarget gSource)) 50)
									)
								(block Nil
									(shpCancelOrders gSource)
									(shpOrder gSource 'gate)
									(objSetData gSource "00003014_behavior" 'onCourse)
									)
								)
						)
					)
			</OnBehavior>
			
			<OnCreateOrders>
				(block (theCourse)
					(if (not aTargetObj)
						(setq aTargeObj gPlayerShip)
						)
						
					; Move towards the player's path
					(setq theCourse (intComputeRandomEncounterCourse gSource aTargetObj))
					(shpOrder gSource 'holdCourse theCourse 300)
					
					; Register a timer so that we can look for enemies
					(sysAddObjRecurringTimerEvent 30 gSource "OnBehavior")
					(objSetData gSource "00003014_behavior" 'onCourse)
					)
			</OnCreateOrders>
			
			<OnOrdersCompleted>
				(block (behavior)
					(setq behavior (objGetData gSource "00003014_behavior"))
					(switch
						; If we're done with this order, then we never encountered
						; the player, so we're done, head for a stargate
						; Note: we might encounter the player en route
						(eq behavior 'onCourse)
							(shpOrder gSource 'gate)
							
						; If we're done attacking, then we're done
						(eq behavior 'attackingEnemy)
							(block Nil
								(shpOrder gSource 'gate)
								(objSetData gSource "00003014_behavior" 'onCourse)
								)
						)
					)
			</OnOrdersCompleted>
		</Events>
	</ShipClass>
	
	<!-- Patrol Behavior
	
		USAGE NOTES
		
		This is a highly configurable patrol behavior class for ships on a cyclic patrol.

		See Properties for configuration details.

		Notes:
			To use a static patrol path, set "rpg.patrol.List" to a list of object IDs in
			the patrol sequence. Then set "rpg.patrol.regenerateOnCycle" to Nil.
	-->
	
	<Type UNID="&evStdPatrolBehavior;">
		<Properties>
			<!-- Config -->
			<Data id="rpg.patrol.maxClusterDistance">
				;	Max distance from a higher priority object that we will search
				;	for lower or equal priority objects to patrol from
				200
			</Data>
			<Data id="rpg.patrol.maxClusterDistRatio">
				;	ratio by which we decrease the max cluster distance for lower
				;	priority objects we treat as being in one cluster
				;	eg:
				;		maxClusterDistance = 200
				;		maxClusterDistRatio = 0.5
				;		maxClusterDistanceP1 = 100
				;		maxClusterDistanceP2 = 50
				;		maxClusterDistanceP3 = 25
				0.5
			</Data>
			<Data id="rpg.patrol.hubPriorityLevel">
				;	At and above this priority level, we treat these objects as
				;	hubs and will return to them after patrolling each lower-priority sub-cluster
				;	Lower-priority clusters are patrolled in a round-robin manner
				1
			</Data>
			<Data id="rpg.patrol.randomOrder">
				;	If non-nil, shuffle the order of objects
				1
			</Data>
			<Data id="rpg.patrol.skipClusteredChildren">
				;	If non-nil, we skip any patrol targets with a parent and no children
				1
			</Data>
			<Data id="rpg.patrol.maxAggroRange">
				;	Max distance we will scan for hostiles from our patrol route
				60
			</Data>
			<Data id="rpg.patrol.aggroScanRate">
				;	Rate at which we scan for hostiles (in ticks)
				60
			</Data>
			<Data id="rpg.patrol.minHealth">
				;	If we are below this health fraction we flee to a friendly station to repair if one exists
				0.2
			</Data>
			<Data id="rpg.patrol.ignorePlayer">
				;	If non-nil, we ignore player aggression
				1
			</Data>
			<Data id="rpg.patrol.regenerateOnCycle">
				;	If non-nil, we dynamically regenerate our patrol
				;	list when completing a patrol cycle
				;
				;	This is useful for patrols that need to adjust to
				;	situations like new stations spawning or existing
				;	stations being destroyed
				;
				;	However it doesnt work with patrols that use
				;	static object lists
				1
			</Data>
			<Data id="rpg.patrol.criteriaP0">
				;	Priority 0 (highest) patrol criteria
				"G"
			</Data>
			<Data id="rpg.patrol.criteriaP1">
				"TAF +primary"
			</Data>
			<Data id="rpg.patrol.criteriaP2">
				"TAF -primary +majorStation"
			</Data>
			<Data id="rpg.patrol.criteriaP3">
				"TAF -primary -majorStation"
			</Data>
			<Data id="rpg.patrol.criteriaP4">
			</Data>

			<!-- Internal state -->

			<Data id="rpg.patrol.ownedMarkers">
			</Data>
			<Data id="rpg.patrol.Idx">
			</Data>
			<Data id="rpg.patrol.State">
			</Data>
			<DynamicData id="rpg.patrol.health">
				(max (/ (obj@ gSource 'hp) (obj@ gSource 'maxHp)) (/ (obj@ gSource 'interiorHP) (obj@ gSource 'maxInteriorHP)))
			</DynamicData>
			<DynamicData id="rpg.patrol.debug">
				(block
					(
						(myVarMutable (list ()))
						myHelper
						myVar
					)
					(setq myHelper (lambda (val) (lnkReplace myVarMutable 0 val)))
					(setq myVar (lambda () (@ myVarMutable 0)))
					(myHelper 1)
					(dbgLog (cat "rpg.patrol.debug test value is: " (myVar)))
					(myVar)
				    )
			</DynamicData>
			<DynamicData id="rpg.patrol.List.generate">
				(block
					(
						;	print debug stats
						(printDebugStats ())

						;	Init vars
						(patrolTargetList (list ))
						(criteriaP0 (obj@ gSource "rpg.patrol.criteriaP0"))
						(criteriaP1 (obj@ gSource "rpg.patrol.criteriaP1"))
						(criteriaP2 (obj@ gSource "rpg.patrol.criteriaP2"))
						(criteriaP3 (obj@ gSource "rpg.patrol.criteriaP3"))
						(criteriaP4 (obj@ gSource "rpg.patrol.criteriaP4"))
						(shuffleOrder (obj@ gSource "rpg.patrol.randomOrder"))

						;	Mutable nil override of sysFindObject
						(fnSysFindObjectMutable (lambda (src criteria shuffle)
							(block
								(
									(result (sysFindObject src criteria))
								)
								(if (=== result)
									; if the result is atomic nil return a mutable nil list instead
									(list )
									; otherwise return as normal
									(if shuffle
										(shuffle result)
										result
									    )
								    )
							    )
						    ))
						
						;	Priority 0 locations include:
						;	stargates
						(patrolTargetListP0
							(fnSysFindObjectMutable gSource criteriaP0)
						    )

						;	Priority 1 locations include:
						(patrolTargetListP1
							(fnSysFindObjectMutable gSource criteriaP1)
						    )

						;	Priority 2 locations include:
						(patrolTargetListP2
							(fnSysFindObjectMutable gSource criteriaP2)
						    )

						;	Priority 3 locations include:
						(patrolTargetListP3
							(fnSysFindObjectMutable gSource criteriaP3)
						    )

						;	Priority 4 locations include:
						;	random position markers in the system
						(patrolTargetListP4
							(fnSysFindObjectMutable gSource criteriaP4)
						    )

						(patrolList (list ))
						(markerList (list ))
						;	these are fallbacks if we dont have enough stuff in the system
						(origin (vector '(0 0)))
						(randomInnerSystemLocation (sysVectorPolarOffset origin (random 0 360) (random 150 300)))
						(randomOuterSystemLocation (sysVectorPolarOffset origin (random 0 360) (random 450 700)))

						;	Compute clusters
						(maxClusterDist (max (obj@ gSource "rpg.patrol.maxClusterDistance") 0.0))
						(maxClusterDistRatio (min (max (obj@ gSource "rpg.patrol.maxClusterDistRatio") 0.0) 1.0))
						(hubPriorityThreshold (obj@ gSource "rpg.patrol.hubPriorityLevel"))
						
						;	Cluster helpers
						(fnPriorityClusterSize (lambda (priority)
							(switch
								; P0 is always the specified max
								(= priority 0)
									maxClusterDist
								; P1 and beyond we compute
								(ls priority 5)
									(* maxClusterDist (pow maxClusterDistRatio priority))
								; error
								(error (cat "Priority cannot be higher than 4. Got: " priority))
							    )
						    ))
						(fnIsHubPriority (lambda (priority)
							(ls priority (+ 1 hubPriorityThreshold))
						    ))
						(fnFindBestParent (lambda (obj objPriority)
							(switch
								;	P0 objects never have parents
								(= objPriority 0)
									()
								(block
									(
										(bestParentP0 (intFindNearestObj obj patrolTargetListP0 (fnPriorityClusterSize 0)))
										(bestParentP1 (if (and (gr objPriority 1) (fnIsHubPriority 1)) (intFindNearestObj obj patrolTargetListP1 (fnPriorityClusterSize 1)) ()))
										(bestParentP2 (if (and (gr objPriority 2) (fnIsHubPriority 2)) (intFindNearestObj obj patrolTargetListP2 (fnPriorityClusterSize 2)) ()))
										(bestParentP3 (if (and (gr objPriority 3) (fnIsHubPriority 3)) (intFindNearestObj obj patrolTargetListP3 (fnPriorityClusterSize 3)) ()))
									)
									(if printDebugStats (dbgLog (cat
										"finding best parent for: " obj
										"\n\tobjPriority: " objPriority
										"\n\tbestParent:"
										"\n\t\tP0: " bestParentP0
										"\n\t\tP1: " bestParentP1
										"\n\t\tP2: " bestParentP2
										"\n\t\tP3: " bestParentP3
										"\n\tIsHubPriority:"
										"\n\t\tP0: " (fnIsHubPriority 0)
										"\n\t\tP1: " (fnIsHubPriority 1)
										"\n\t\tP2: " (fnIsHubPriority 2)
										"\n\t\tP3: " (fnIsHubPriority 3)
										"\n\t\tP4: " (fnIsHubPriority 4)
										"\n\tPriorityClusterSize:"
										"\n\t\tP0: " (fnPriorityClusterSize 0)
										"\n\t\tP1: " (fnPriorityClusterSize 1)
										"\n\t\tP2: " (fnPriorityClusterSize 2)
										"\n\t\tP3: " (fnPriorityClusterSize 3)
										"\n\t\tP4: " (fnPriorityClusterSize 4)
									    )))
									;	we always try to pick the lowest parent
									(switch
										bestParentP3
											bestParentP3
										bestParentP2
											bestParentP2
										bestParentP1
											bestParentP1
										bestParentP0
											bestParentP0
										()
									    )
								    )
							    )
						    ))
						(fnObjKey (lambda (obj) (cat "" (objGetID obj))))
						(fnObjFromKey (lambda (key) (objGetObjByID (int key))))
						(parentMap (block
							(
								(parentMap { })
							)
							;	Priority 0 keys are always nil
							(enum patrolTargetListP1 patrolObj (set@ parentMap (fnObjKey patrolObj) (fnFindBestParent patrolObj 1)))
							(enum patrolTargetListP2 patrolObj (set@ parentMap (fnObjKey patrolObj) (fnFindBestParent patrolObj 2)))
							(enum patrolTargetListP3 patrolObj (set@ parentMap (fnObjKey patrolObj) (fnFindBestParent patrolObj 3)))
							(enum patrolTargetListP4 patrolObj (set@ parentMap (fnObjKey patrolObj) (fnFindBestParent patrolObj 4)))
							parentMap
						    ))
						(parentMapKeys (map
							(append
								patrolTargetListP0
								patrolTargetListP1
								patrolTargetListP2
								patrolTargetListP3
								patrolTargetListP4
							)
							patrolObj
							(fnObjKey patrolObj)
						    ))
						(priorityMap (block
							(
								(priorityMap { })
							)
							(enum patrolTargetListP0 patrolObj (set@ priorityMap (fnObjKey patrolObj) 0))
							(enum patrolTargetListP1 patrolObj (set@ priorityMap (fnObjKey patrolObj) 1))
							(enum patrolTargetListP2 patrolObj (set@ priorityMap (fnObjKey patrolObj) 2))
							(enum patrolTargetListP3 patrolObj (set@ priorityMap (fnObjKey patrolObj) 3))
							(enum patrolTargetListP4 patrolObj (set@ priorityMap (fnObjKey patrolObj) 4))
							priorityMap
						    ))
						(patrolHubs (map parentMapKeys 'excludeNil objKey (if (@ parentMap objKey) () (fnObjFromKey objKey))))
						(fnFindClosestHigherHub (lambda (obj objPriority)
							(switch
								(= objPriority 0)
									()
								(intFindNearestObj obj
									(map (@ patrolHubs) 'excludeNil objKey
										(if (gr (@ priorityMap key) objPriority)
											;;	Ignore all objects with worse priority
											()
											;;	Otherwise the object is a candidate
											(fnObjFromKey key)
										    )
									    )
								    )
							    )
						    ))
						(unparentedHubs (list ))
						(patrolHubMap (block
							(
								(patrolHubMap { })
								(skipChildOnly (obj@ gSource "rpg.patrol.skipClusteredChildren"))
								(hubKey (fnObjKey hubObj))
							)
							;	populate keys and empty lists
							
							(enum patrolHubs hubObj (set@ patrolHubMap hubKey (list )))
							(if printDebugStats (dbgLog (cat "patrolHubMap initial: " patrolHubMap)))

							;	populate hubs that are parented to other hubs in the lists of the parent hub
							
							(enum patrolHubs hubObj
								(block
									(
										(higherObj (fnFindClosestHigherHub hubObj (@ priorityMap hubKey)))
										(objKey (fnObjKey higherObj))
										(childList (@ patrolHubMap objKey))
									)
									(if higherObj
										(if childList
											(lnkAppend childList hubObj)
											;	we need to use this workaround because set@ doesnt add nils to structs...
											(set@ patrolHubMap (list hubObj))
										    )
										;	Record any hubs that have no parent
										(lnkAppend unparentedHubs hubObj)
									    )
								    )
							    )
							(if printDebugStats (dbgLog (cat "patrolHubMap parented: " patrolHubMap)))
							
							;	if we dont skip them add parented objects with no children
							
							(if skipChildOnly
								;	skip
								()
								;	otherwise add them to their immediate parent
								(enum
									(map parentMapKeys '(excludeNil original) objKey (@ parentMap key))
									childObjectKey
									(if (@ patrolHubs childObjectKey)
										;	If this has children it was already added
										()
										;	If it has no children we need to add it
										(block
											(
												(objKey (fnObjKey (@ parentMap childObjectKey)))
												(childObj (fnObjFromKey childObjectKey))
												(childList (@ patrolHubMap objKey))
											)
											(if childList
												(lnkAppend childList childObj)
												;	we need to use this workaround because set@ doesnt add nils to structs...
												(set@ patrolHubMap (list childObj))
											    )
										    )
									    )
								    )
							    )
							(if printDebugStats (dbgLog (cat "patrolHubMap final: " patrolHubMap)))
							
							;	return the final map
							
							patrolHubMap
						    ))

						;	init patrol path
						(patrolPath (list ))

						;	helper fn
						(fnGreedySortFrom (lambda (startObj destObjList)
							(block
								(
									;	Copy the list since we will mutate it and also remove startobj if present
									(destObjs (map destObjList 'excludeNil destObj (if (= destObj startObj) () destObj)))
									(sortedObjs (list ))
									(curObj startObj)
								)
								(while destObjs
									(block
										(
											(nearestObj (intFindNearestObj curObj destObjs))
										)
										(lnkAppend sortedObjs nearestObj)
										(lnkRemove destObjs (find destObjs nearestObj))
										(setq curObj nearestObj)
									    )
								    )
								sortedObjs
							    )
						    ))
						(fnAddToPatrolPath (lambda (hubObj)
							(block
								(
									(childObjsToAdd (@ patrolHubMap (objKey hubObj)))
									(sortedChildObjsToAdd (fnGreedySortFrom (if shuffleOrder (random childObjsToAdd) hubObj) childObjsToAdd))
								)
								(lnkAppend patrolPath hubObj)
								(if childObjsToAdd
									(block ()
										;	Go to each child object as a hub
										(enum childObjsToAdd childObj
											(fnAddToPatrolPath childObj)
										    )
										;	Return to the hub object
										(lnkAppend patrolPath hubObj)
									    )
								    )
							    )
						    ))

						;	get root hubs to start from
						(rootHubs (fnGreedySortFrom (random unparentedHubs) unparentedHubs))
					    )

					;	Generate patrol path
					(enum rootHubs rootHub (fnAddToPatrolPath rootHub))

					;	Debug print
					(if printDebugStats
						(dbgLog (cat
							"parent map:\t" parentMap
							"\npriority map:\t" priorityMap
							"\npatrol hubs:\t" patrolHubs
							"\npatrol hub map:\t" patrolHubMap
							"\npatrol path:\t" patrolPath
							"\nunparented hubs:\t" unparentedHubs
							"\nroot hubs:\t" rootHubs
						    ))
					    )

					;	Convert from pointers to spaceObj IDs

					(map patrolPath patrolObj (objGetID patrolObj))
				    )
			</DynamicData>

			<!-- Patrol list -->

			<Data id="rpg.patrol.List">
				(obj@ gSource "rpg.patrol.List.generate")
			</Data>
		</Properties>
		<Events>
			<OnEventHandlerInit>
				(block
					(
						(posList (obj@ gSource "rpg.patrol.List"))
						(nextObjID (@ posList 0))
						(navObj (objGetObjByID nextObjID))
					)
					(objSet@ gSource "rpg.patrol.Idx" 0)
					(objSet@ gSource "rpg.patrol.State" 'patrol)
					(shpOrder gSource 'approach navObj 10)
					(sysAddObjRecurringTimerEvent (obj@ gSource "rpg.patrol.aggroScanRate") gSource "rpg.patrolScan")
				    )
			</OnEventHandlerInit>
			<rpg.patrolScan>
				(block
					(
						(state (obj@ gSource "rpg.patrol.State"))
						(scanRange (obj@ gSource "rpg.patrol.maxAggroRange"))
					)
					(switch
						;	We only scan and attack hostiles while on patrol

						(= state "patrol")
							(block
								(
									(ignorePlayer (obj@ gSource "rpg.patrol.ignorePlayer"))
									;	Note that we only look for ships we can actually see
									(baseHostileCriteria (if ignorePlayer "sAEZP" "sAEP"))
									(rangedHostileCriteria (if (gr scanRange 0) (cat baseHostileCriteria " NN:" scanRange) (cat baseHostileCriteria " N")))
									(nearestHostileShip (sysFindObject gSource rangedHostileCriteria))
								)
								(if nearestHostileShip
									(block ()
										(shpCancelOrders gSource)
										(objSet@ gSource "rpg.patrol.State" "attacking")
										(shpOrder gSource 'attack nearestHostileShip)
									    )
								    )
							    )

						;	Ensure our target hasnt escaped

						(= state "attacking")
							(if
								(gr
									(sysVectorDistance (objGetPos gSource) (objGetPos (objGetTarget gSource)))
									(* scanRange 1.5)
								    )
								;	Break off our attack if it got away
								;	we let onOrdersCompleted run to resume course
								(shpCancelOrders gSource)
							    )

						;	Otherwise we continue on as normal
						
						()
					    )
				    )
			</rpg.patrolScan>
			<OnOrdersCompleted>
				(block
					(
						(state (obj@ gSource "rpg.patrol.State"))
					)
					(switch
						;	If we are patrolling, we continue to the next location
						(= state "patrol")
							(block
								(
									(idx (obj@ gSource "rpg.patrol.Idx"))
									(posList (obj@ gSource "rpg.patrol.List"))
									(nextIdx (if (geq (+ 1 idx) (count posList)) 0 (+ 1 idx)))
									(regenerateOnCycle (obj@ gSource "rpg.patrol.regenerateOnCycle"))
									(setNewPosList (if (and (= nextIdx 0) regenerateOnCycle) (objSet@ gSource "rpg.patrol.List" (obj@ gSource "rpg.patrol.List.generate"))))
									(finalPosList (obj@ gSource "rpg.patrol.List"))
									(nextObjID (@ finalPosList nextIdx))
									(navObj (objGetObjByID nextObjID))
								)
								(objSet@ gSource "rpg.patrol.State" 'patrol)
								(objSet@ gSource "rpg.patrol.Idx" nextIdx)
								(shpOrder gSource 'approach navObj 10)
							    )
						;	If we fled to repair, we complete repairs then continue to the next location
						(= state "flee")
							(block
								(
									(idx (obj@ gSource "rpg.patrol.Idx"))
									(posList (obj@ gSource "rpg.patrol.List"))
									(objID (@ posList idx))
									(navObj (objGetObjByID objID))
								)
								(shpFullRestore gSource)
								(objSet@ gSource "rpg.patrol.State" 'patrol)
								(shpOrder gSource 'approach navObj 10)
							    )
						;	If we were interrupted we resume course
						(block
							(
								(idx (obj@ gSource "rpg.patrol.Idx"))
								(posList (obj@ gSource "rpg.patrol.List"))
								(objID (@ posList idx))
								(navObj (objGetObjByID objID))
							)
							(objSet@ gSource "rpg.patrol.State" 'patrol)
							(shpOrder gSource 'approach navObj 10)
						    )
					    )
				    )
			</OnOrdersCompleted>
			<OnAttacked>
				(block
					(
						(state (obj@ gSource "rpg.patrol.State"))
						(ignorePlayer (obj@ gSource "rpg.patrol.ignorePlayer"))
						(wasTargeted (= gSource (objGetTarget aOrderGiver)))
					)
					(switch
						;	Keep fleeing if already fleeing
						(or (= state "flee") (= state "fleeGate"))
							()
						
						;	If our health is too low we flee
						(ls (obj@ gSource "rpg.patrol.health") (obj@ gSource "rpg.patrol.minHealth"))
							(block
								(
									(safeHarbor (srvFindRepairArmor gSource 60 (itmGetLevel (shpGetArmor gSource 0))))
								)
								(if safeHarbor
									;	Find a safe location to repair
									(block ()
										(objSet@ gSource "rpg.patrol.State" "flee")
										(shpCancelOrders gSource)
										(shpOrder gSource 'dock safeHarbor)
										(shpOrder gSource 'wait (random 3 8))
									    )
									;	Otherwise flee for a gate if one exists
									(block ()
										(objSet@ gSource "rpg.patrol.State" "fleeGate")
										(shpCancelOrders gSource)
										(shpOrder gSource 'gate)
									    )
								    )
							    )
						
						;	If we are configured to ignore the player we do so
						(and ignorePlayer (= aOrderGiver gPlayerShip))
							()

						;	If we are fighting already then keep fighting
						(or (= state "retaliating") (= state "attacking"))
							()

						;	If no one is reponsible, then nothing to do

						(not (objCanAttack aOrderGiver))
							()

						;	If we were deliberately targeted, retaliate even if they are friendly

						wasTargeted
							(block ()
								(objSet@ gSource "rpg.patrol.State" "retaliating")
								(shpCancelOrders gSource)
								(shpOrder gSource 'attack aOrderGiver)
							    )

						;	If this is a friend, then ignore since it was just friendly fire probably

						(= (objGetDisposition gSource aOrderGiver) 'friend)
							()

						;	Otherwise we retaliate

						(block ()
							(objSet@ gSource "rpg.patrol.State" "retaliating")
							(shpCancelOrders gSource)
							(shpOrder gSource 'attack aOrderGiver)
						    )
					    )
				    )
			</OnAttacked>
			<OnDestroy>
				;	Cleanup any nav markers
				(enum (obj@ gSource "rpg.patrol.ownedMarkers") ownedMarker (objRemove (objGetObjByID ownedMarker)))
			</OnDestroy>
		</Events>
	</Type>

	<!-- Resupply Behavior
	
		This behavior is for freighters resupplying a station when needed. The
		ship will enter the system at some stargate loaded with supplies. It 
		will dock with its base and transfer supplies. Then it will gate out.

		USAGE

		We expect the station to have the following properties:

		rpg.suppliesNeeded: This is a list of items for the station.
	
	-->

	<Type unid="&evStdResupplyBehavior;">
		<Events>
			<OnCreateOrders>

				;	aBaseObj is our base station.

				(block (
					(suppliesNeeded (obj@ aBaseObj 'rpg.suppliesNeeded))
					)
					;	Remember the list of supplies, so we don't deliver extra
					;	stuff. Then add all the items to the ship.

					(objSetData gSource 'rpg.resupplyList suppliesNeeded)
					(enum suppliesNeeded theItem
						(objAddItem gSource theItem)
						)

					;	Remember our base

					(objSetData gSource 'rpg.baseID (objGetID aBaseObj))

					;	Order to dock with base

					(if aBaseObj
						(shpOrder gSource 'dock aBaseObj)
						)

					;(dbgOutput "Resupply " (objGetID aBaseObj) " with " (itmGetName (@ suppliesNeeded 0) 'count))
					)
			</OnCreateOrders>

			<OnOrdersCompleted>
				(switch
					;	If we're docked at our station, then dump our items and
					;	leave the system.

					(= (obj@ gSource 'dockedAtID) (objGetData gSource 'rpg.baseID))
						(block (
							(suppliesToTransfer (objGetData gSource 'rpg.resupplyList))
							(baseObj (objGetObjByID (objGetData gSource 'rpg.baseID)))
							)
							(enum suppliesToTransfer theItem
								(block ()
									(objRemoveItem gSource theItem)
									(objAddItem baseObj theItem)

									;(dbgOutput "Transfered " (itmGetName theItem 'count) " to " (objGetID baseObj))
									)
								)

							;	Wait and then gate out

							(shpOrder gSource 'wait (random 3 8))
							(shpOrder gSource 'gate)
							)
					)
			</OnOrdersCompleted>
		</Events>
	</Type>

	<!-- Wander Behavior
	
		USAGE NOTES

		Use this for wandering encounters, such as Xenophobe fleets. Ships will
		wander around the system avoiding enemy stations, but fighting back when
		threats come too close.

		If the ship is an aggressor (aggressor="true" in AI settings) then it 
		will retaliate against stations when attacked. Otherwise, it will not.
	
	-->

	<Type unid="&evStdWanderBehavior;">
		<Properties>
			<Definition id="wander.angleInc">60</Definition>

			<Data id="wander.centerID">(objGetID (sysFindObject gSource "tN +star"))</Data>
			<Data id="wander.radius">(random 200 900)</Data>

			<!-- wander.status
			
				Status can be one of the following values:

				'wandering: Ship is wandering around the system.
				'attacking: Ship is attacking a target.

			-->
			<Data id="wander.status">'wandering</Data>

			<Data id="wander.lastEnemyCheck"></Data>
			<Data id="wander.testRadius">0</Data>
			<Variant id="wander.maxCR">(obj@ gSource 'challengeRating)</Variant>
		</Properties>

		<Events>
			<OnEventHandlerInit>
				(block ()
					;	Deter any ship that gets too close

					(shpSetAISetting gSource 'reactToThreat 'deterNoManeuvers)
					(shpSetAISetting gSource 'threatRange 20)
					)
			</OnEventHandlerInit>

			<OnAttacked>
				(block (
					(status (obj@ gSource 'wander.status))

					allTargets
					filteredTargets
					)
					(switch
						;	Only if we're aggressive

						(not (shpGetAISetting gSource 'aggressor))
							Nil

						;	If we're not wandering, then ignore

						(!= status 'wandering)
							Nil

						;	If no order giver, nothing to do.

						(not aOrderGiver)
							Nil

						;	If order giver is not an enemy, then nothing

						(not (objIsEnemy gSource aOrderGiver))
							Nil

						;	Don't check too often

						(gr (+ (obj@ gSource 'wander.lastEnemyCheck) 150) (unvGetTick))
							Nil

						;	Only if we're healthy

						(gr (objGetVisibleDamage gSource) 50)
							Nil

						;	Look for an enemy station matching the sovereign of the
						;	order giver.

						(not (setq allTargets (sysFindObject aOrderGiver "TA J N:100 +populated; | sA J N:50")))
							Nil

						;	Filter down to those that are visible to us and have
						;	a reasonable challenge rating.

						(not (setq filteredTargets
								(filter allTargets theObj
									(and (leq (obj@ theObj 'challengeRating) (obj@ gSource 'wander.maxCR))
											(objCanDetectTarget gSource theObj)
											(gr (obj@ gSource 'maxSpeed) (obj@ theObj 'maxSpeed))
											)
									)
								))
							Nil

						;	Attack the given target

						(block (
							(targetObj (random filteredTargets))
							)
							(shpCancelOrders gSource)
							(shpOrder gSource 'attackOrRetreat targetObj)
							(objSet@ gSource 'wander.status 'attacking)
							)
						)
					)
			</OnAttacked>

			<OnOrdersCompleted>
				(block (
					(centerObj (objGetObjByID (obj@ gSource 'wander.centerID)))
					(radius (obj@ gSource 'wander.radius))

					;	Compute our angle relative to the center.

					(curAngle (sysVectorAngle gSource centerObj))

					;	Advance to the next point in the orbit

					(newAngle (+ curAngle (obj@ gSource 'wander.angleInc)))
					(newPoint (sysVectorPolarOffset centerObj newAngle radius))

					;	Figure out the next point on our path. We advance along the
					;	orbit and find a point away from any station.

					(radiusFunc (lambda ()
						(block (
							(radius (obj@ gSource 'wander.testRadius))
							)
							(objInc@ gSource 'wander.testRadius 20)
							radius
							)
						))
					(reset (objSet@ gSource 'wander.testRadius 0))
					(newDest (sysVectorRandom newPoint radiusFunc 100 "sTA"))
					)
					(switch
						;	If no center object then nothing to do

						(not centerObj)
							Nil

						;	Navigate to the new destination

						(block ()
							(shpOrder gSource 'gotoPos newDest)
							(objSet@ gSource 'wander.status 'wandering)
							)
						)
					)
			</OnOrdersCompleted>
		</Events>
	</Type>
	
	<!-- Zone Encounter Behavior
	
		USAGE NOTES
		
		This is for random encounters in which the player runs into
		a patrol that is orbiting a station. The patrol will ignore enemies
		unless the enemies are within 300 light-seconds of the station.
		
		Note: This only works with enemies that have their own sovereign.
		
		EXTRA DATA
		
		behavior:			Ship's current behavior
								'onCourse				= Ship on straight course
								'backToBase				= Heading back to base
								'patrolling				= Orbiting base
								'leavingSystem			= Leaving
								
		homeObj:			Ship's base (may be Nil)
		
	-->

	<ShipClass UNID="&evStdZoneEncounterBehavior;"
			class=				"(zone encounter behavior)"
			virtual=			"true"
			
			attributes=			"behaviorClass"
			>
			
		<Events>
			<GetCreatePos>
				(if aTargetObj
					(intComputeRandomEncounterPos aTargetObj)
					)
			</GetCreatePos>

			<OnBehavior>
				(block (behavior)
					(setq behavior (objGetData gSource "00003016_behavior"))
					(switch
						; If we see any enemy ships and we're inside our patrol zone
						; then attack. Otherwise, change course and head back to base.

						(eq behavior 'onCourse)
							(block (theEnemy theBase baseDist)
								(if (setq theEnemy (sysFindObject gSource "sEPAN"))
									(switch

										; Attack
										(and (setq theBase (objGetObjRefData gSource "00003016_homeObj"))
												(leq (setq baseDist (objGetDistance gSource theBase)) 300)
												)
											(block Nil
												(shpCancelOrders gSource)
												(shpOrder gSource 'patrol theBase 300)
												(objSetData gSource "00003016_behavior" 'patrolling)
												)

										; Back to base
										theBase
											(block Nil
												(shpCancelOrders gSource)
												(shpOrder gSource 'holdCourse
													(sysVectorSubtract (objGetPos theBase) (objGetPos gSource))
													(subtract baseDist 300)
													)
												(objSetData gSource "00003016_behavior" 'backToBase)
												)

										; Leave the system
										(block Nil
											(shpCancelOrders gSource)
											(shpOrder gSource 'gate)
											(objSetData gSource "00003016_behavior" 'leavingSystem)
											)
										)
									)
								)
						)
					)
			</OnBehavior>
			
			<OnCreateOrders>
				(block (theCourse homeBase)
					(if (not aTargetObj)
						(setq aTargeObj gPlayerShip)
						)

					; Find our nearest base
					(setq homeBase (sysFindObject gSource "TAFJN"))
					(if (not homeBase)
						(setq homeBase aBaseObj)
						)
					(objSetObjRefData gSource "00003016_homeObj" homeBase)

					; Move towards the player's path
					(setq theCourse (intComputeRandomEncounterCourse gSource aTargetObj))
					(shpOrder gSource 'holdCourse theCourse 300)

					; Register a timer so that we can look for enemies
					(sysAddObjRecurringTimerEvent 30 gSource "OnBehavior")
					(objSetData gSource "00003016_behavior" 'onCourse)
					)
			</OnCreateOrders>
			
			<OnOrdersCompleted>
				(block (behavior)
					(setq behavior (objGetData gSource "00003016_behavior"))
					(switch
						; If we're done with this order, then we never encountered
						; the player, so we're done, head for a stargate
						; Note: we might encounter the player en route
						(eq behavior 'onCourse)
							(shpOrder gSource 'gate)
							
						; If we're done attacking, then we're done
						(eq behavior 'attackingEnemy)
							(block Nil
								(shpOrder gSource 'gate)
								(objSetData gSource "00003016_behavior" 'onCourse)
								)

						(eq behavior 'backToBase)
							(block Nil
								(shpOrder gSource 'patrol (objGetObjRefData gSource "00003016_homeObj") 300)
								(objSetData gSource "00003016_behavior" 'patrolling)
								)
						)
					)
			</OnOrdersCompleted>
		</Events>
	</ShipClass>

</TranscendenceModule>
